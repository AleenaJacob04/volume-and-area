#include<iostream>
using namespace std;
class rectangle{
	private:
		double width;
		double length;
	public:
		double area();
	    double get(){
		cout<<"enter the width";
		cin>>width;
		cout<<"enter the length";
		cin>>length;
	}
		
};
double rectangle::area(){
	return length*width;
	
}
	class cubic::pubic rectangle{
		private:
		double height;
		
public:
 double get(){
   cout<<"Enter the height:";
   cin>>height;
}
	}; 
double.volume(){
	return area()*height;
	
}
int main(){
	rectangle rl;
	rl.get();
	cout<<rl.area()<<endl;
	cuboid c1;
	c1.get();
	cout<<cl.volume();
}

